# PPO Configuration for Box2D Environments

# LunarLander-v3 configuration
LunarLander-v3:
  episodes: 1000
  learning_rate: 0.0003
  discount_factor: 0.99
  gae_lambda: 0.95
  clip_epsilon: 0.2
  entropy_coef: 0.01
  value_coef: 0.5
  max_grad_norm: 0.5
  batch_size: 64
  n_steps: 2048
  n_epochs: 10
  max_episode_steps: 1000

# CarRacing-v3 configuration
CarRacing-v3:
  episodes: 1500
  learning_rate: 0.00025
  discount_factor: 0.99
  gae_lambda: 0.95
  clip_epsilon: 0.2
  entropy_coef: 0.001
  value_coef: 0.5
  max_grad_norm: 0.5
  batch_size: 128
  n_steps: 2048
  n_epochs: 4
  max_episode_steps: 1000
